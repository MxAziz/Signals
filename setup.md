1. first install python.
2. check: python --version

```powershell
pip install numpy matplotlib scipy
```

othoba `pip` kaj na korle:

```powershell
python -m pip install numpy matplotlib scipy
```

### tarpor check koro

```powershell
python -c "import numpy, matplotlib, scipy; print('Everything is OK!')"
```

jodi dekhai:

```text
Everything is OK!
```


```powershell
python experiment_01.py
```

to see the output graph

** simplest setup:**

```text
Python install
      ↓
VS Code
      ↓
pip install numpy matplotlib scipy
      ↓
experiment_01.py
      ↓
python experiment_01.py
```